This directory contains html and javascript files used by webView. Since .gitignore will prevent 
html files from being archived in the github repository, html files for Gooble Maps should have 
valid API keys. 

The ./Resources directory has the template versions where the literal 'MYAPIKEY' shouuld be
replaces with the actual keys and the modified file copied to this directory.
